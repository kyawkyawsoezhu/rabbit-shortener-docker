# Start Server

* Run the following commands
    *  `docker-compose build`
    *  `docker-compose up -d`

if you have confilt port with your host machine, please change port in .env file.

this setup also include phpMyAdmin.
